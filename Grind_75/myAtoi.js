var myAtoi = function(s) {
    // set up empty string variable
    // set up a false flag
    // iterate over string
        // if '-' is found an the next char is a num
            //turn false flag to true
        // else if current place can be turned into a number
            // add current to string variable
            //loop from the current location
                // until current string char cant be a number
                // add them to string variable
            //break
    //if flag
        //return string turned into a number times -1
    //else return string numbered
};